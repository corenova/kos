import joint from 'jointjs'
import React, { Component } from 'react'

export default class Reactor extends Component {

  render() {
    const { graph, reactor } = this.props

    graph.addCell([].concat(inputs, handler, outputs, links))

    return null
  }

}
